#  now taking user's input mean, asking user what they want to fill in particular space. 
# this is done using input() function which is a built-in function in python.

# to take a string input 
name = input("Enter your name: ") # this will take the input from the user and store it in the variable name


#  similarly we can take input for age and country
age = int(input("Enter your age: "))

country = input("Enter your country: ")
print(f"{name} is {age} years old and is from {country}.")

#  you can takeuser input and perform operation, that we will be doing in next stages
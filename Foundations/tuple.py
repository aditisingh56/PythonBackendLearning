three_numbers = (1, 2, 3)
print(three_numbers) # this will print the tuple (1, 2, 3)
#  we can also have different data types in a tuple
mixed_tuple = (1, "Aditi", 3.14, True)

print(type(three_numbers)) # this will print the type of data stored in the variable three_numbers which is tuple

# we can alos create tuple usuing its constructor 

tuple1 = tuple((1, 2, 3, 4, 5)) # this will create a tuple with the elements 1, 2, 3, 4 and 5